version: 2
jobs:
  build:
    docker:
      - image: gcc:8
    steps:
      - run: git clone https://github.com/kometbomb/klystron.git && git clone https://github.com/kometbomb/klystrack.git
      - run: cd klystrack && make
